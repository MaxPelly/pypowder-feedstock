{% set version = "0.0.0a6" %}

package:
  name: pypowder
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/P/PyPowder/pypowder-{{ version }}.tar.gz
  sha256: 63201190afe31ed78f1a6685bebcb6cbe8c3bf9fb2f41d5f4a28f76d5248a44a

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=77.0.3
    - setuptools-scm >=9.2
    - pip
  run:
    - python >={{ python_min }}
    - pandas
    - numpy
    - xmltodict
    - matplotlib-base

test:
  imports:
    - PyPOW
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/MaxPelly/PyPOW
  summary: Attempt at constructing a set of tools to convert XRDML files to a more usable format.
  license: GPL-3.0-or-later
  license_file: LICENCE

extra:
  recipe-maintainers:
    - MaxPelly
